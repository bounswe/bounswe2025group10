# ==============================================================================
# Zero Waste Challenge - Mobile App Environment Configuration
# ==============================================================================
# Copy this file to .env and update the values for your environment
#
# IMPORTANT: Never commit .env to version control!
# ==============================================================================

# ------------------------------------------------------------------------------
# Backend API Configuration
# ------------------------------------------------------------------------------

# API_URL - The base URL for the backend API
#
# Choose one of the following based on your setup:

# Option 1: Production Server (Recommended for release builds)
# Use this for production builds and when connecting to the live backend
API_URL=https://zerowaste.ink

# Option 2: Local Development with Docker Compose
# Use this when running the backend via docker-compose on the same machine
# Note: 'localhost' won't work from an Android emulator or physical device
# API_URL=http://localhost:8000

# Option 3: Android Emulator connecting to host machine
# Android emulator uses 10.0.2.2 to access the host's localhost
# API_URL=http://10.0.2.2:8000

# Option 4: iOS Simulator connecting to host machine
# iOS simulator can use localhost directly
# API_URL=http://localhost:8000

# Option 5: Physical Device on same network
# Replace with your computer's local IP address (run 'ipconfig' or 'ifconfig')
# API_URL=http://192.168.1.100:8000

# Option 6: Docker Compose Service Name
# When both mobile and backend run in Docker Compose, use the service name
# API_URL=http://backend:8000

# ------------------------------------------------------------------------------
# Network Configuration Notes
# ------------------------------------------------------------------------------
#
# IMPORTANT: The mobile app runs on a device/emulator that has its own network
# stack. "localhost" from the app's perspective is the device itself, NOT your
# development machine.
#
# Quick Reference:
# | Environment              | API_URL Value                    |
# |--------------------------|----------------------------------|
# | Production               | https://zerowaste.ink            |
# | Android Emulator         | http://10.0.2.2:8000             |
# | iOS Simulator            | http://localhost:8000            |
# | Physical Device (WiFi)   | http://<your-machine-ip>:8000    |
# | Docker Compose           | http://backend:8000              |
#
# To find your machine's IP address:
# - macOS/Linux: ifconfig | grep "inet " | grep -v 127.0.0.1
# - Windows: ipconfig | findstr /i "IPv4"
#
# See MOBILE_BUILD.md for detailed network configuration instructions.
# ==============================================================================
